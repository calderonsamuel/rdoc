# Parse primary type (qualified identifier with optional element type and length)

Parse primary type (qualified identifier with optional element type and
length)

## Usage

``` r
parse_primary_type(parser)
```
