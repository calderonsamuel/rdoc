# Normalize box.path entries to absolute paths

Normalize box.path entries to absolute paths

## Usage

``` r
normalize_box_paths(paths, project_root)
```

## Arguments

- paths:

  Character vector of paths (may be relative)

- project_root:

  Project root directory

## Value

Character vector of absolute paths
