# Parameter Type Information

Represents type information for a single function parameter.

## Usage

``` r
param_type(type = character(0), description = character(0))
```

## Arguments

- type:

  Character string with the type specification (e.g., "numeric1")

- description:

  Character string describing the parameter
