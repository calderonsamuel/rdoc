# Extract properties from S7::new_class call

Extract properties from S7::new_class call

## Usage

``` r
extract_s7_properties(class_expr, env)
```

## Arguments

- class_expr:

  Expression containing S7::new_class call

- env:

  Environment

## Value

Named list of param_type objects or NULL
