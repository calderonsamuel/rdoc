# Function Type Signature

Complete type signature for a function, including all parameters and the
return type.

## Usage

``` r
function_signature(params = list(), return = NULL)
```

## Arguments

- params:

  Named list of param_type objects (names are parameter names)

- return:

  return_type object or NULL if no return type specified
