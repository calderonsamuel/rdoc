# Parse a single (non-union) type

Parse a single (non-union) type

## Usage

``` r
parse_single_type(type_string)
```

## Arguments

- type_string:

  A single type string

## Value

A list with base type and optional length constraint
