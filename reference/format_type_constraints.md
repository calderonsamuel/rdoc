# Format type specification for display

Converts parsed type specification back to string format

## Usage

``` r
format_type_constraints(parsed_type)
```

## Arguments

- parsed_type:

  Parsed type specification from parse_type_constraints()

## Value

Character string
