# Validate AST semantic rules

Note: S7 validators already enforce most invariants at construction
time. This function is kept for recursive validation of nested
structures.

## Usage

``` r
ast_validate(ast)
```
