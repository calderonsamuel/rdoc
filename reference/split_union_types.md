# Split union type into components

Split union type into components

## Usage

``` r
split_union_types(type_string)
```

## Arguments

- type_string:

  Union type string

## Value

Character vector of individual types
