# Parse a type specification

Parses type strings like "numeric(1)", "character \| NULL", etc.

## Usage

``` r
parse_type_spec(type_string)
```

## Arguments

- type_string:

  Character string with type specification

## Value

A list with parsed type information
