# Tokenize type syntax string

Breaks a type syntax string into tokens for parsing.

## Usage

``` r
lex_type_syntax(input)
```

## Arguments

- input:

  Character string containing type syntax

## Value

List of token objects, each with: type, value, position
