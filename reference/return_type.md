# Return Type Information

Represents type information for a function's return value.

## Usage

``` r
return_type(type = character(0), description = character(0))
```

## Arguments

- type:

  Character string with the type specification

- description:

  Character string describing the return value
