% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linter-box.R
\name{extract_box_imports}
\alias{extract_box_imports}
\title{Extract box::use() import statements from XML AST}
\usage{
extract_box_imports(xml)
}
\arguments{
\item{xml}{XML parsed content from lintr}
}
\value{
List of module imports, each with structure:
list(
module_path = "mod/math",           # The module path as written
module_name = "math",                # Last component for lookup
alias = "m" or NULL,                 # User-provided alias
imports = c("add", "multiply") or NULL,  # NULL = full module import
attach_all = FALSE,                  # TRUE if ... syntax used
line = 5L                            # Line number of box::use() call
)
}
\description{
Parses all box::use() calls in the source code and extracts:
\itemize{
\item Module paths (e.g., "mod/math", "./local/utils")
\item Import aliases (e.g., "m" in \code{box::use(m = mod/math)})
\item Selective imports (e.g., \code{c("add", "multiply")})
\item Attach-all imports (e.g., \code{...} syntax)
}
}
\examples{
\dontrun{
# box::use(mod/math) → full module import
# box::use(m = mod/math) → aliased import
# box::use(mod/math\[add, multiply\]) → selective import
# box::use(mod/math\[...\]) → attach all
}
}
\keyword{internal}
